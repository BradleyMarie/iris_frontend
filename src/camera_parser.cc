#include <iostream>

#include "src/camera_parser.h"

namespace iris {

CameraConfig ParseCamera(Tokenizer& tokenizer, MatrixManager& matrix_manager) {
  std::cerr << "ERROR: ParseCamera not implemented" << std::endl;
  exit(EXIT_FAILURE);
}

}  // namespace iris